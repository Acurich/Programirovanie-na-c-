//В чемпионате по футболу команде за выигрыш даётся 3 очка, за проигрыш — 0, за ничью — 1. Известно количество очков, полученных командой за игру. Определите словесный результат игры (выигрыш, проигрыш или ничья).
#include <iostream>
using namespace std;
int main() {
 int a;
cin >> a;
if (a==3)
{
cout << "WIN";
}
else if (a==0)
{
cout << "LOSE";
}
else if (a==1)
{
cout << "DRAW";
}
else cout << "Unknown";
return 0;
}

//Дано целое число m, задающее номер месяца года. Выведите строку — название времени года, соответствующего данному месяцу.
#include <iostream>
using namespace std;
int main() {
  int m;
  cin >> m;
  if (m == 12 or m == 1 or m == 2)
    cout << "WINTER";
  if (m == 3 or m == 4 or m == 5)
    cout << "SPRING";
  if (m == 6 or m == 7 or m == 8)
    cout << "SUMMER";
  if (m == 9 or m == 10 or m == 11)
    cout << "AUTUMN";
  return 0;
}

//Мальчик-романтик из царства-государства дарит цветы незнакомым дамам-девушкам. Известно, что он дарит цветок каждой пятой даме-девушке, но при этом не дарит цветок, если порядковый номер дамы-девушки кратен 25. А вот если порядковый номер кратен ста, то мальчик-романтик все-таки дарит цветок, несмотря на то, что 25-ти этот номер тоже кратен. Известен порядковый номер N дамы-девушки, определите, подарит ли ей цветок мальчик-романтик.
#include <iostream>
using namespace std;
int main() {
  int n;
  cin >> n;
  if (n % 100 == 0 || (n % 25 != 0 && n % 5 == 0))
    cout << "YES" << endl;
  else
    cout << "NO" << endl;

  return 0;
}

//В царстве-государстве решено было собрать с людей-богачей пожертвования на благотворительность. Все согласились, что пожертвование должен сделать самый богатый из людей-богачей. Известны финансовые состояния четырех людей-богачей A, B, C, D. Определите, сколько денег останется у каждого из них после пожертвования, если сумма пожертвования составляет N дубиков.
#include <iostream>
using namespace std;
int main() {
  int a, b, c, d, n;
  cin >> a >> b >> c >> d >> n;
  if (a > b && a > c && a > d)
    a -= n;
  else if (b > a && b > c && b > d)
    b -= n;
  else if (c > a && c > b && c > d)
    c -= n;
  else
    d -= n;
  cout << a << " " << b << " " << c << " " << d << endl;
  return 0;
}

//В царстве-государстве решено было собрать с людей-богачей пожертвования на благотворительность. Все согласились, что пожертвование должен сделать самый богатый из людей-богачей. Известны финансовые состояния четырех людей-богачей A, B, C, D. Определите, сколько денег останется у каждого из них после пожертвования, если сумма пожертвования составляет N дубиков. Если по каким-то причинам богатейший из людей-богачей обанкротится в процессе пожертвования, то оставшиеся дубики должен будет заплатить следующий богатейший, а если обанкротится и он, то следующий после него и так далее.
#include <iostream>
using namespace std;
int main() {
  int a, b, c, d, e;
  cin >> a >> b >> c >> d >> e;
  if ((a>b) and (a>c) and (a>d)) 
 cout << a=a-e;
else if ((b>a) and (b>c) and (b>d)) 
 b=b-e;
else if ((c>a) and (c>b) and (c>d)) 
 c=c-e;
else if ((d>a) and (d>b) and (d>c)) 
 d=d-e;
cout << a << " " << b << " " << c << " " << d; 
if ((a<=0) && (b>a) and (b>c) and (b>d)) 
 b=b-e;
else if ((c>a) and (c>b) and (c>d)) 
 c=c-e;
else if ((d>a) and (d>b) and (d>c)) 
 d=d-e;
cout << a << " " << b << " " << c << " " << d; 
if ((b<=0) && (c>a) and (c>b) and (c>d))
c=c-e;
else if ((d>a) and (d>b) and (d>c)) 
d=d-e;
cout << a << " " << b << " " << c << " " << d; 
if ((c<=0) && (d>a) and (d>b) and (d>c))
d=d-e;
cout << a << " " << b << " " << c << " " << d;
  return 0;
}
